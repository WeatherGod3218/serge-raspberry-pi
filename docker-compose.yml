---

services:
  serge-raspberry-pi:
    build: .
    container_name: Serge-Raspberry-Pi
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data

    # devices:
    #   - /dev/i2c-1:/dev/i2c-1
    # environment:
    #   - BLINKA_RASPBERRY_PI=1
